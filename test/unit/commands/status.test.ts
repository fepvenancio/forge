import { describe, it } from "vitest";

describe("forge status", () => {
  it.todo("CLI-04: displays team-wide status with developer table");
  it.todo("CLI-04: shows summary counts (active, PRs open, blocked)");
  it.todo("CLI-04: shows idle status for developers without assignments");
});
