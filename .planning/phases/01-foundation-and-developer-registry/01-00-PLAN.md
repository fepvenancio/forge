---
phase: 01-foundation-and-developer-registry
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/commands/register.test.ts
  - test/integration/developer-registry.test.ts
  - test/unit/commands/phases.test.ts
  - test/unit/commands/sync.test.ts
  - test/unit/commands/status.test.ts
autonomous: true
requirements: [IDENT-01, IDENT-02, IDENT-03, PHASE-04, CLI-03, CLI-04]

must_haves:
  truths:
    - "All Wave 0 test stub files exist and can be discovered by vitest"
    - "Each test stub has a describe block with skipped test cases matching requirement IDs"
    - "Running vitest on any stub file succeeds (all tests skipped, zero failures)"
  artifacts:
    - path: "test/unit/commands/register.test.ts"
      provides: "Test stubs for forge register and forge team commands"
      contains: "describe"
    - path: "test/integration/developer-registry.test.ts"
      provides: "Integration test stubs for developer persistence"
      contains: "describe"
    - path: "test/unit/commands/phases.test.ts"
      provides: "Test stubs for forge phases command"
      contains: "describe"
    - path: "test/unit/commands/sync.test.ts"
      provides: "Test stubs for forge sync command"
      contains: "describe"
    - path: "test/unit/commands/status.test.ts"
      provides: "Test stubs for forge status command"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files required by the Validation Strategy. These stubs ensure vitest can discover and run (as skipped) all test files before implementation begins, satisfying Nyquist sampling continuity.

Purpose: Establishes test scaffolding so that Plans 01-01, 01-02, and 01-03 can reference real test files in their `<automated>` verify commands.
Output: 5 test stub files with describe blocks and skipped test cases.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-developer-registry/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Wave 0 test stub files</name>
  <files>test/unit/commands/register.test.ts, test/integration/developer-registry.test.ts, test/unit/commands/phases.test.ts, test/unit/commands/sync.test.ts, test/unit/commands/status.test.ts</files>
  <action>
Create the `test/unit/commands/` and `test/integration/` directories if they do not exist.

**Create `test/unit/commands/register.test.ts`** with:
```typescript
import { describe, it } from "vitest";

describe("forge register", () => {
  it.todo("IDENT-01: registers current git user as a developer");
  it.todo("IDENT-01: uses --name override when provided");
  it.todo("IDENT-01: errors when git user.name not configured and no --name given");
  it.todo("IDENT-01: updates last_active if already registered");
});

describe("forge team", () => {
  it.todo("IDENT-02: lists all registered developers in a table");
  it.todo("IDENT-02: shows message when no developers registered");
});
```

**Create `test/integration/developer-registry.test.ts`** with:
```typescript
import { describe, it } from "vitest";

describe("developer registry integration", () => {
  it.todo("IDENT-03: registerDeveloper persists to Dolt with timestamps");
  it.todo("IDENT-03: getDeveloper retrieves persisted developer");
  it.todo("IDENT-03: updateDeveloperActivity updates last_active timestamp");
  it.todo("IDENT-03: duplicate registration throws error");
});
```

**Create `test/unit/commands/phases.test.ts`** with:
```typescript
import { describe, it } from "vitest";

describe("forge phases", () => {
  it.todo("PHASE-04: displays all phase assignments in a table");
  it.todo("PHASE-04: shows message when no assignments exist");
  it.todo("PHASE-04: formats status with color coding");
});
```

**Create `test/unit/commands/sync.test.ts`** with:
```typescript
import { describe, it } from "vitest";

describe("forge sync", () => {
  it.todo("CLI-03: displays current phase assignments from Dolt");
  it.todo("CLI-03: shows developer and assignment counts");
  it.todo("CLI-03: formats assignment table with status colors");
});
```

**Create `test/unit/commands/status.test.ts`** with:
```typescript
import { describe, it } from "vitest";

describe("forge status", () => {
  it.todo("CLI-04: displays team-wide status with developer table");
  it.todo("CLI-04: shows summary counts (active, PRs open, blocked)");
  it.todo("CLI-04: shows idle status for developers without assignments");
});
```

All test cases use `it.todo()` so they are recognized by vitest as pending but do not fail.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && test -f test/unit/commands/register.test.ts && test -f test/integration/developer-registry.test.ts && test -f test/unit/commands/phases.test.ts && test -f test/unit/commands/sync.test.ts && test -f test/unit/commands/status.test.ts && grep -q "IDENT-01" test/unit/commands/register.test.ts && grep -q "IDENT-03" test/integration/developer-registry.test.ts && grep -q "PHASE-04" test/unit/commands/phases.test.ts && grep -q "CLI-03" test/unit/commands/sync.test.ts && grep -q "CLI-04" test/unit/commands/status.test.ts && pnpm vitest run test/unit/commands/register.test.ts test/unit/commands/phases.test.ts test/unit/commands/sync.test.ts test/unit/commands/status.test.ts test/integration/developer-registry.test.ts --passWithNoTests 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>All 5 Wave 0 test stub files exist with todo test cases matching requirement IDs. Vitest discovers them without errors.</done>
</task>

</tasks>

<verification>
- All 5 files exist in their respective directories
- Each file contains `describe` and `it.todo` with requirement ID references
- `pnpm vitest run` on all 5 files completes without errors (todos are not failures)
</verification>

<success_criteria>
- 5 test stub files created matching VALIDATION.md Wave 0 requirements
- Vitest can discover and run (skip) all stubs without failure
- Requirement IDs IDENT-01, IDENT-02, IDENT-03, PHASE-04, CLI-03, CLI-04 referenced in test names
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-developer-registry/01-00-SUMMARY.md`
</output>
