---
phase: 03-event-driven-enforcement
plan: 0
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/coordination/merge-engine.test.ts
  - test/unit/coordination/escalation.test.ts
  - test/unit/commands/merge-order.test.ts
  - test/unit/webhook/push-handler.test.ts
  - test/unit/gates/ci-gates.test.ts
autonomous: true
requirements:
  - CI-02
  - CI-03
  - CI-05
  - CI-06
  - MERGE-01
  - MERGE-02
  - MERGE-03
  - MERGE-04
  - ESC-01
  - ESC-02
  - FLOW-02
  - FLOW-03

must_haves:
  truths:
    - "All five test stub files exist and contain describe blocks with it.todo() placeholders"
    - "Running pnpm test passes (stubs do not break existing tests)"
  artifacts:
    - path: "test/unit/coordination/merge-engine.test.ts"
      provides: "Test stubs for merge order computation"
      contains: "computeMergeOrder"
    - path: "test/unit/coordination/escalation.test.ts"
      provides: "Test stubs for conflict escalation"
      contains: "escalateConflicts"
    - path: "test/unit/commands/merge-order.test.ts"
      provides: "Test stubs for merge-order CLI command"
      contains: "merge-order"
    - path: "test/unit/webhook/push-handler.test.ts"
      provides: "Test stubs for push event handling and merge order comment posting"
      contains: "push"
    - path: "test/unit/gates/ci-gates.test.ts"
      provides: "Test stubs for CI gate logic including criticality-based freshness"
      contains: "ci-gates"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 3 test files.

Purpose: Establish the test scaffold so subsequent plans can implement against concrete test expectations. All stubs use `it.todo()` to mark pending tests.
Output: 5 test files with describe/it.todo structure covering all Phase 3 requirements.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-event-driven-enforcement/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Phase 3 test stub files</name>
  <files>
    test/unit/coordination/merge-engine.test.ts,
    test/unit/coordination/escalation.test.ts,
    test/unit/commands/merge-order.test.ts,
    test/unit/webhook/push-handler.test.ts,
    test/unit/gates/ci-gates.test.ts
  </files>
  <read_first>
    test/unit/coordination/conflict-detector.test.ts,
    test/unit/coordination/lock-manager.test.ts,
    test/unit/commands/conflicts.test.ts,
    test/unit/webhook/pr-conflict-handler.test.ts
  </read_first>
  <action>
Create 5 test stub files following the exact pattern from Phase 2 tests (vi.mock with .js extensions, vitest imports).

**test/unit/coordination/merge-engine.test.ts:**
```
import { describe, it } from "vitest";

describe("merge-engine", () => {
  describe("computeMergeOrder", () => {
    it.todo("returns empty order for empty input");
    it.todo("returns single phase when only one phase exists");
    it.todo("orders phases by declared dependencies (ROADMAP dependsOn)");
    it.todo("adds inferred edges from touch map overlaps (lower phase first)");
    it.todo("detects circular dependencies and returns them in cycles array");
    it.todo("handles dependency chains (A -> B -> C)");
    it.todo("includes reasoning strings explaining ordering decisions");
    it.todo("filters to only phases with open PRs when prNumber is set");
  });
});
```

**test/unit/coordination/escalation.test.ts:**
```
import { describe, it } from "vitest";

describe("escalation", () => {
  describe("escalateConflicts", () => {
    it.todo("returns false when FORGE_ESCALATION_WEBHOOK_URL is not set");
    it.todo("sends POST to webhook URL with conflict details");
    it.todo("includes file paths, phase IDs, and developer names in payload");
    it.todo("returns true on successful webhook response");
    it.todo("returns false and logs error on fetch failure");
    it.todo("formats message with all conflicting files and phases");
  });
});
```

**test/unit/commands/merge-order.test.ts:**
```
import { describe, it } from "vitest";

describe("merge-order command", () => {
  it.todo("displays merge order table when phases have open PRs");
  it.todo("shows 'no open PRs' message when no phases have pr_open status");
  it.todo("displays cycle warnings when circular dependencies detected");
  it.todo("shows reasoning for merge order decisions");
});
```

**test/unit/webhook/push-handler.test.ts:**
```
import { describe, it } from "vitest";

describe("push event handler", () => {
  it.todo("ignores push events for non-GSD branches");
  it.todo("extracts changed files from push payload commits");
  it.todo("marks matching flows as stale in Dolt");
  it.todo("handles push with no matching flow file refs");
  it.todo("returns success even when Dolt is unavailable");

  describe("merge order PR comment", () => {
    it.todo("posts merge order PR comment when multiple PRs are open");
    it.todo("uses <!-- forge-merge-order --> marker for update-or-create");
    it.todo("skips merge order comment when fewer than 2 PRs are open");
  });

  describe("escalation wiring", () => {
    it.todo("triggers escalateConflicts when conflict report has conflicts");
    it.todo("does not fail webhook response when escalation fails");
  });
});
```

**test/unit/gates/ci-gates.test.ts:**
```
import { describe, it } from "vitest";

describe("CI gate scripts", () => {
  describe("touch_map_conflicts gate", () => {
    it.todo("exits 0 when no conflicts detected");
    it.todo("exits 1 when conflicts found and outputs conflict details");
    it.todo("gracefully skips when Dolt is unavailable");
  });

  describe("config_validation gate", () => {
    it.todo("exits 0 when config.json is valid");
    it.todo("exits 1 when config.json has invalid branching_strategy");
    it.todo("exits 1 when config.json is missing required fields");
  });

  describe("cost_audit gate", () => {
    it.todo("exits 0 with warning when no cost data exists");
    it.todo("exits 0 when costs are within budget");
    it.todo("gracefully skips when Dolt is unavailable");
  });

  describe("flow_freshness criticality", () => {
    it.todo("uses criticality level for freshness threshold");
    it.todo("applies shorter threshold for critical flows than low flows");
  });

  describe("PR comment formatting", () => {
    it.todo("formats gate results as markdown table");
    it.todo("includes forge-gates-report marker comment for update-or-create pattern");
  });
});
```

Ensure each file uses the exact same import style as Phase 2 tests. Directory `test/unit/gates/` may need to be created.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && pnpm --filter @forge/orchestrator test</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "computeMergeOrder" test/unit/coordination/merge-engine.test.ts
    - grep -l "escalateConflicts" test/unit/coordination/escalation.test.ts
    - grep -l "merge-order" test/unit/commands/merge-order.test.ts
    - grep -l "push" test/unit/webhook/push-handler.test.ts
    - grep -l "forge-merge-order" test/unit/webhook/push-handler.test.ts
    - grep -l "escalateConflicts" test/unit/webhook/push-handler.test.ts
    - grep -l "criticality" test/unit/gates/ci-gates.test.ts
    - grep -l "ci-gates\|CI gate" test/unit/gates/ci-gates.test.ts
    - grep -c "it.todo" test/unit/coordination/merge-engine.test.ts returns 8
    - grep -c "it.todo" test/unit/coordination/escalation.test.ts returns 6
    - grep -c "it.todo" test/unit/webhook/push-handler.test.ts returns 10
    - grep -c "it.todo" test/unit/gates/ci-gates.test.ts returns 13
    - All existing tests still pass
  </acceptance_criteria>
  <done>All 5 test files exist with it.todo() stubs covering all Phase 3 requirements including FLOW-03 criticality and MERGE-03 merge order comments. pnpm test passes.</done>
</task>

</tasks>

<verification>
- All 5 test stub files exist in their respective directories
- `pnpm --filter @forge/orchestrator test` passes (stubs are inert)
- Each file contains describe blocks matching the implementation functions they will test
- ci-gates.test.ts has a "flow_freshness criticality" describe block (FLOW-03)
- push-handler.test.ts has "merge order PR comment" and "escalation wiring" describe blocks (MERGE-03, ESC-01)
</verification>

<success_criteria>
Wave 0 test scaffold is in place for all Phase 3 implementation plans to reference.
</success_criteria>

<output>
After completion, create `.planning/phases/03-event-driven-enforcement/03-00-SUMMARY.md`
</output>
