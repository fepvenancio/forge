---
phase: 04-team-dashboard-and-cost-tracking
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/dolt/cost-queries.test.ts
  - test/unit/commands/cost.test.ts
  - test/unit/commands/dashboard.test.ts
autonomous: true
requirements: [COST-01, COST-02, COST-03, COST-04, DASH-06]

must_haves:
  truths:
    - "Test stubs exist for all cost query functions"
    - "Test stubs exist for forge cost CLI command"
    - "Test stubs exist for forge dashboard CLI command"
  artifacts:
    - path: "test/unit/dolt/cost-queries.test.ts"
      provides: "Cost query function test stubs"
      contains: "describe"
    - path: "test/unit/commands/cost.test.ts"
      provides: "Cost CLI command test stubs"
      contains: "describe"
    - path: "test/unit/commands/dashboard.test.ts"
      provides: "Dashboard CLI command test stubs"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stubs for Phase 4 cost tracking and dashboard features.

Purpose: Establish test scaffolding that Plan 01 (cost tracking) and Plan 02 (dashboard) will implement against. Nyquist compliance requires test files to exist before production code.
Output: Three test files with describe blocks and it.todo stubs.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-team-dashboard-and-cost-tracking/04-RESEARCH.md
@.planning/phases/04-team-dashboard-and-cost-tracking/04-VALIDATION.md

<interfaces>
<!-- Existing test pattern from merge-order.test.ts -->
From test/unit/commands/merge-order.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
vi.mock("../../../packages/orchestrator/src/dolt/queries.js", () => ({ ... }));
// Extract action handler from commander mock, call it, assert console.log output
```

From packages/orchestrator/src/dolt/schema.ts:
```typescript
export interface DeveloperCost {
  id: number;
  developer_id: string;
  phase_id: number;
  model: string;
  input_tokens: number;
  output_tokens: number;
  cost_usd: number;
  recorded_at: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for cost queries, cost CLI, and dashboard CLI</name>
  <files>
    test/unit/dolt/cost-queries.test.ts
    test/unit/commands/cost.test.ts
    test/unit/commands/dashboard.test.ts
  </files>
  <read_first>
    test/unit/commands/merge-order.test.ts
    packages/orchestrator/src/dolt/schema.ts
    .planning/phases/04-team-dashboard-and-cost-tracking/04-VALIDATION.md
  </read_first>
  <action>
Create three test stub files following the exact vitest pattern from merge-order.test.ts (import from vitest, vi.mock with .js extensions, describe/it.todo).

**test/unit/dolt/cost-queries.test.ts** (COST-01):
Create the `test/unit/dolt/` directory first. Then create cost-queries.test.ts with:
- `vi.mock("../../../packages/orchestrator/src/dolt/client.js", ...)` mocking `query`, `execute`, `transaction`
- `describe("developer cost queries", ...)` with these it.todo stubs:
  - `it.todo("recordDeveloperCost inserts row and returns DeveloperCost")`
  - `it.todo("getDeveloperCosts returns costs for a specific developer")`
  - `it.todo("getPhaseCosts returns costs for a specific phase")`
  - `it.todo("getTeamCostSummary returns grouped cost data")`
  - `it.todo("getPhaseTotalCost returns sum of costs for a phase")`

**test/unit/commands/cost.test.ts** (COST-02, COST-03, COST-04):
- `vi.mock("../../../packages/orchestrator/src/dolt/queries.js", ...)` mocking cost query functions
- `describe("cost command", ...)` with these it.todo stubs:
  - `it.todo("registers cost command on a program")`
  - `it.todo("--team flag shows team-wide cost summary")`
  - `it.todo("--dev flag shows costs for a specific developer")`
  - `it.todo("shows budget alert when phase cost exceeds FORGE_PHASE_BUDGET_USD")`
  - `it.todo("cost record subcommand records a new cost entry")`
  - `it.todo("legacy cycle-id argument falls back to cycle cost display")`
  - `it.todo("cost-per-PR shows costs correlated with PR numbers")`

**test/unit/commands/dashboard.test.ts** (DASH-06):
- `describe("dashboard command", ...)` with these it.todo stubs:
  - `it.todo("registers dashboard command on a program")`
  - `it.todo("spawns streamlit with correct app.py path")`
  - `it.todo("passes --port option to streamlit")`
  - `it.todo("shows error message when streamlit not installed")`
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && npx vitest run test/unit/dolt/cost-queries.test.ts test/unit/commands/cost.test.ts test/unit/commands/dashboard.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "describe" test/unit/dolt/cost-queries.test.ts returns the file
    - grep -l "describe" test/unit/commands/cost.test.ts returns the file
    - grep -l "describe" test/unit/commands/dashboard.test.ts returns the file
    - grep -c "it.todo" test/unit/dolt/cost-queries.test.ts returns 5
    - grep -c "it.todo" test/unit/commands/cost.test.ts returns 7
    - grep -c "it.todo" test/unit/commands/dashboard.test.ts returns 4
    - npx vitest run on all three files exits 0 (todos are skipped, not failures)
  </acceptance_criteria>
  <done>All three test stub files exist with correct describe blocks and it.todo entries. vitest runs clean with all todos skipped.</done>
</task>

</tasks>

<verification>
npx vitest run test/unit/dolt/cost-queries.test.ts test/unit/commands/cost.test.ts test/unit/commands/dashboard.test.ts --reporter=verbose
All tests should show as skipped/todo, zero failures.
</verification>

<success_criteria>
- Three test stub files created in correct directories
- All it.todo stubs map to Phase 4 requirements (COST-01 through COST-04, DASH-06)
- vitest runs clean (exit 0)
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-dashboard-and-cost-tracking/04-00-SUMMARY.md`
</output>
