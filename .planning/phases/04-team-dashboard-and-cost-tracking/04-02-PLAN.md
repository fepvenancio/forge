---
phase: 04-team-dashboard-and-cost-tracking
plan: 02
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - packages/dashboard/app.py
  - packages/dashboard/pages/phase_map.py
  - packages/dashboard/pages/pr_status.py
  - packages/dashboard/pages/merge_order.py
  - packages/dashboard/pages/file_locks.py
  - packages/dashboard/pages/team_costs.py
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, COST-05]

must_haves:
  truths:
    - "Dashboard shows phase ownership table with developer names and status"
    - "Dashboard shows PR status for all phase branches with links"
    - "Dashboard shows merge order recommendation using Kahn's algorithm"
    - "Dashboard shows file lock map with owner and phase info"
    - "Dashboard shows cost per developer and per phase with charts"
    - "All dashboard pages handle missing Dolt connection gracefully"
  artifacts:
    - path: "packages/dashboard/app.py"
      provides: "Updated main entry with v2 branding and navigation"
      contains: "Team Coordination"
    - path: "packages/dashboard/pages/phase_map.py"
      provides: "Phase ownership view"
      contains: "phase_assignments"
    - path: "packages/dashboard/pages/pr_status.py"
      provides: "PR status view"
      contains: "pr_number"
    - path: "packages/dashboard/pages/merge_order.py"
      provides: "Merge order recommendation view"
      contains: "compute_merge_order"
    - path: "packages/dashboard/pages/file_locks.py"
      provides: "File lock map view"
      contains: "file_locks"
    - path: "packages/dashboard/pages/team_costs.py"
      provides: "Cost per developer/phase view"
      contains: "developer_costs"
  key_links:
    - from: "packages/dashboard/pages/phase_map.py"
      to: "Dolt phase_assignments table"
      via: "pymysql SELECT"
      pattern: "phase_assignments"
    - from: "packages/dashboard/pages/team_costs.py"
      to: "Dolt developer_costs table"
      via: "pymysql SELECT"
      pattern: "developer_costs"
    - from: "packages/dashboard/pages/merge_order.py"
      to: "Kahn's algorithm"
      via: "compute_merge_order function"
      pattern: "def compute_merge_order"
---

<objective>
Create five new Streamlit dashboard pages for team coordination views and update the main app.py with v2 branding.

Purpose: Gives the team visual access to phase ownership, PR status, merge order, file locks, and cost data -- all reading from existing Dolt v2 tables.
Output: Five new Python pages in packages/dashboard/pages/ and an updated app.py.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-team-dashboard-and-cost-tracking/04-RESEARCH.md

<interfaces>
<!-- Existing dashboard pattern from task_board.py and app.py -->
From packages/dashboard/app.py:
```python
import streamlit as st
import pymysql
import os
st.set_page_config(page_title="Forge Dashboard", page_icon="...", layout="wide")
conn = pymysql.connect(host=os.environ.get("DOLT_HOST", "localhost"), ...)
```

From packages/dashboard/pages/task_board.py (established page pattern):
```python
# Each page is self-contained with its own get_connection()
def get_connection():
    return pymysql.connect(
        host=os.environ.get("DOLT_HOST", "localhost"),
        port=int(os.environ.get("DOLT_PORT", "3306")),
        user=os.environ.get("DOLT_USER", "root"),
        password=os.environ.get("DOLT_PASSWORD", ""),
        database=os.environ.get("DOLT_DATABASE", "forge"),
        charset="utf8mb4",
        cursorclass=pymysql.cursors.DictCursor,
    )
```

<!-- Dolt v2 tables these pages query -->
Tables: developers, phase_assignments, file_locks, developer_costs
Timestamps are BIGINT epoch milliseconds (divide by 1000 for Python datetime.fromtimestamp)

<!-- Merge engine algorithm reference -->
From packages/orchestrator/src/coordination/merge-engine.ts:
```typescript
export function computeMergeOrder(phases: PhaseNode[]): MergeOrder
// PhaseNode: { phaseId, dependsOn, branch, prNumber, touchedFiles }
// MergeOrder: { order, cycles, reasoning }
// Uses Kahn's algorithm with inferred edges from touch map overlaps
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create five Streamlit dashboard pages and update app.py</name>
  <files>
    packages/dashboard/pages/phase_map.py
    packages/dashboard/pages/pr_status.py
    packages/dashboard/pages/merge_order.py
    packages/dashboard/pages/file_locks.py
    packages/dashboard/pages/team_costs.py
    packages/dashboard/app.py
  </files>
  <read_first>
    packages/dashboard/pages/task_board.py
    packages/dashboard/pages/cost_tracker.py
    packages/dashboard/app.py
    packages/orchestrator/src/coordination/merge-engine.ts
    .planning/phases/04-team-dashboard-and-cost-tracking/04-RESEARCH.md
  </read_first>
  <action>
Create five new Streamlit pages and update app.py. Each page MUST follow the established pattern:
- Self-contained `get_connection()` with DictCursor (do NOT extract to shared module -- see research anti-patterns)
- `st.set_page_config(page_title="...", layout="wide")`
- try/except around all Dolt queries with user-friendly error messages via `st.error()`
- Timestamps: always `datetime.fromtimestamp(ts / 1000)` (epoch milliseconds, not seconds)

**1. packages/dashboard/pages/phase_map.py (DASH-01):**
- Title: "Phase Map"
- Query: `SELECT pa.phase_id, pa.assignee, d.display_name, pa.status, pa.branch_name, pa.pr_number, pa.assigned_at FROM phase_assignments pa LEFT JOIN developers d ON pa.assignee = d.id ORDER BY pa.phase_id ASC`
- Display as `st.dataframe` with columns: Phase, Developer, Status, Branch, PR#, Assigned
- Add `st.metric` cards at top: Total Phases, Active (in_progress), PRs Open, Merged
- Status column should use color coding via pandas styling if possible, or display raw status values

**2. packages/dashboard/pages/pr_status.py (DASH-02):**
- Title: "PR Status"
- Query: `SELECT pa.phase_id, pa.assignee, d.display_name, pa.status, pa.branch_name, pa.pr_number FROM phase_assignments pa LEFT JOIN developers d ON pa.assignee = d.id WHERE pa.pr_number IS NOT NULL ORDER BY pa.pr_number DESC`
- Display as `st.dataframe` with columns: PR#, Phase, Developer, Status, Branch
- Show `st.info("No open PRs found")` when empty

**3. packages/dashboard/pages/merge_order.py (DASH-03):**
- Title: "Merge Order"
- Re-implement Kahn's algorithm in Python (~30 lines, do NOT shell out to forge merge-order):
  ```python
  def compute_merge_order(phases):
      # phases: list of dicts with keys: phase_id, depends_on (list of int), touched_files (list of str)
      # Returns: dict with order (list of int), cycles (list of list of int), reasoning (list of str)
  ```
  Follow the algorithm from the research document (04-RESEARCH.md Kahn's Algorithm in Python section) exactly.
- Query phase_assignments for all active phases (status NOT IN ('merged'))
- For touched_files: query `SELECT DISTINCT tm.file_path FROM touch_maps tm JOIN tasks t ON tm.task_id = t.id` (or show empty if no touch map data)
- For depends_on: not easily available from Dolt, so use empty list (dependency info is in plan files, not DB). The touch map overlap will still produce useful ordering.
- Display: numbered list showing merge order, reasoning section, cycle warnings with `st.warning`

**4. packages/dashboard/pages/file_locks.py (DASH-04):**
- Title: "File Locks"
- Query: `SELECT fl.file_path, fl.locked_by, d.display_name, fl.phase_id, fl.locked_at, fl.reason FROM file_locks fl LEFT JOIN developers d ON fl.locked_by = d.id ORDER BY fl.locked_at DESC`
- Display as `st.dataframe` with columns: File, Locked By, Phase, Locked At, Reason
- Show `st.info("No active file locks")` when empty
- Add `st.metric` showing total lock count

**5. packages/dashboard/pages/team_costs.py (DASH-05, COST-05):**
- Title: "Team Costs"
- Query 1 (summary): `SELECT developer_id, phase_id, SUM(cost_usd) as total_cost, SUM(input_tokens) as total_input, SUM(output_tokens) as total_output, COUNT(*) as record_count FROM developer_costs GROUP BY developer_id, phase_id ORDER BY developer_id, phase_id`
- Query 2 (by developer): `SELECT developer_id, SUM(cost_usd) as total FROM developer_costs GROUP BY developer_id`
- Query 3 (by phase): `SELECT phase_id, SUM(cost_usd) as total FROM developer_costs GROUP BY phase_id`
- Display: `st.metric` for total team cost. `st.bar_chart` for cost by developer and cost by phase (use pandas DataFrame). `st.dataframe` for detailed summary table.
- Budget alert: read `FORGE_PHASE_BUDGET_USD` from env. If set and any phase exceeds it, show `st.warning(f"Phase {phase_id} exceeds budget: ${total:.2f} > ${budget:.2f}")`
- Show `st.info("No cost data recorded yet")` when empty

**6. Update packages/dashboard/app.py:**
- Change subtitle from `"*Autonomous Engineering Factory*"` to `"*Team Coordination Layer*"`
- Update Welcome markdown to list both v2 and legacy views:
  - Team Coordination (v2): Phase Map, PR Status, Merge Order, File Locks, Team Costs
  - Legacy Views (v0.1.0): Task Board, Audit Trail, Cost Tracker
- Update Quick Start: `forge register`, `forge claim <N>`, monitor here
- Keep Dolt connection check unchanged
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && python3 -c "import ast; [ast.parse(open(f'packages/dashboard/pages/{p}').read()) for p in ['phase_map.py','pr_status.py','merge_order.py','file_locks.py','team_costs.py']]; ast.parse(open('packages/dashboard/app.py').read()); print('All 6 files parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "phase_assignments" packages/dashboard/pages/phase_map.py returns matches
    - grep "pr_number" packages/dashboard/pages/pr_status.py returns matches
    - grep "compute_merge_order" packages/dashboard/pages/merge_order.py returns matches
    - grep "file_locks" packages/dashboard/pages/file_locks.py returns matches
    - grep "developer_costs" packages/dashboard/pages/team_costs.py returns matches
    - grep "get_connection" packages/dashboard/pages/phase_map.py returns matches (self-contained connection)
    - grep "/ 1000" packages/dashboard/pages/phase_map.py returns matches (epoch ms conversion)
    - grep "Team Coordination" packages/dashboard/app.py returns matches
    - grep "Connected to Dolt" packages/dashboard/app.py returns matches (connection check preserved)
    - All 6 files parse without syntax errors
  </acceptance_criteria>
  <done>Five Streamlit dashboard pages and updated app.py created. Each page is self-contained with get_connection(), queries correct Dolt v2 tables, handles errors gracefully, and uses appropriate Streamlit components. app.py shows v2 branding with complete navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Checkpoint: Visual verification of dashboard</name>
  <files>packages/dashboard/app.py</files>
  <action>
Human visually verifies the 5 new Streamlit dashboard pages and updated app.py. No code changes -- this is a visual verification checkpoint.

Steps for verification:
1. Ensure Dolt is running (`scripts/init-dolt.sh` or check port 3306)
2. Run `cd packages/dashboard && streamlit run app.py`
3. Verify the landing page shows "Team Coordination Layer" and lists all 8 views
4. Click through each new page in the sidebar:
   - Phase Map: shows table (may be empty if no assignments)
   - PR Status: shows table or "No open PRs" message
   - Merge Order: shows order or empty state
   - File Locks: shows table or "No active file locks"
   - Team Costs: shows charts/metrics or "No cost data" message
5. Verify all pages handle Dolt being unavailable gracefully (stop Dolt, refresh page, should show error not crash)
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && python3 -c "import ast; [ast.parse(open(f'packages/dashboard/pages/{p}').read()) for p in ['phase_map.py','pr_status.py','merge_order.py','file_locks.py','team_costs.py']]; ast.parse(open('packages/dashboard/app.py').read()); print('All pages parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 Python files parse without syntax errors
    - Human confirms pages render correctly in browser
  </acceptance_criteria>
  <done>Human confirms all 5 dashboard pages render correctly, show appropriate data or empty states, and handle Dolt unavailability gracefully.</done>
</task>

</tasks>

<verification>
1. All 5 new Python pages parse without syntax errors
2. app.py parses without syntax errors
3. All pages follow self-contained get_connection() pattern
4. All pages handle Dolt connection errors gracefully
5. Visual verification via running dashboard (checkpoint)
</verification>

<success_criteria>
- 5 new Streamlit pages in packages/dashboard/pages/
- app.py updated with v2 branding and complete navigation
- Each page queries correct Dolt v2 tables
- Merge order page has Python Kahn's algorithm implementation
- All pages handle errors gracefully (no crashes on missing Dolt)
- Visual checkpoint confirms pages render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-dashboard-and-cost-tracking/04-02-SUMMARY.md`
</output>
