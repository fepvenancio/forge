---
phase: 04-team-dashboard-and-cost-tracking
plan: 01
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - packages/orchestrator/src/dolt/queries.ts
  - packages/orchestrator/src/commands/cost.ts
  - packages/orchestrator/src/commands/dashboard.ts
  - packages/orchestrator/src/cli.ts
  - test/unit/dolt/cost-queries.test.ts
  - test/unit/commands/cost.test.ts
  - test/unit/commands/dashboard.test.ts
autonomous: true
requirements: [COST-01, COST-02, COST-03, COST-04, DASH-06]

must_haves:
  truths:
    - "Developer costs can be recorded in Dolt with model, tokens, USD, and phase"
    - "forge cost --team shows team-wide cost summary grouped by developer and phase"
    - "forge cost --dev <name> shows costs for a specific developer"
    - "forge cost record <model> <input> <output> <cost> --phase <N> records a cost entry"
    - "Budget alert displays when phase cost exceeds FORGE_PHASE_BUDGET_USD threshold"
    - "forge dashboard launches Streamlit on configurable port"
    - "Legacy forge cost <cycle-id> still works for backward compatibility"
  artifacts:
    - path: "packages/orchestrator/src/dolt/queries.ts"
      provides: "Developer cost CRUD functions"
      contains: "recordDeveloperCost"
    - path: "packages/orchestrator/src/commands/cost.ts"
      provides: "forge cost CLI command module"
      exports: ["costCommands"]
    - path: "packages/orchestrator/src/commands/dashboard.ts"
      provides: "forge dashboard CLI command module"
      exports: ["dashboardCommands"]
    - path: "packages/orchestrator/src/cli.ts"
      provides: "Updated CLI wiring with cost and dashboard commands"
      contains: "costCommands"
  key_links:
    - from: "packages/orchestrator/src/commands/cost.ts"
      to: "packages/orchestrator/src/dolt/queries.ts"
      via: "import * as queries"
      pattern: "queries\\.recordDeveloperCost|queries\\.getTeamCostSummary|queries\\.getDeveloperCosts"
    - from: "packages/orchestrator/src/cli.ts"
      to: "packages/orchestrator/src/commands/cost.ts"
      via: "import { costCommands }"
      pattern: "costCommands\\(program\\)"
    - from: "packages/orchestrator/src/cli.ts"
      to: "packages/orchestrator/src/commands/dashboard.ts"
      via: "import { dashboardCommands }"
      pattern: "dashboardCommands\\(program\\)"
---

<objective>
Implement cost tracking data layer, CLI commands (forge cost + forge dashboard), and wire them into the CLI.

Purpose: Enables developers to record costs, view cost reports (team-wide or per-developer), get budget alerts, and launch the dashboard. This is the TypeScript backend for Phase 4.
Output: Cost query functions in queries.ts, cost.ts and dashboard.ts command modules, updated cli.ts, and passing tests.
</objective>

<execution_context>
@/Users/address0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/address0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-team-dashboard-and-cost-tracking/04-RESEARCH.md
@.planning/phases/04-team-dashboard-and-cost-tracking/04-00-SUMMARY.md

<interfaces>
<!-- Key types the executor needs -->
From packages/orchestrator/src/dolt/schema.ts:
```typescript
export interface DeveloperCost {
  id: number;
  developer_id: string;
  phase_id: number;
  model: string;
  input_tokens: number;
  output_tokens: number;
  cost_usd: number;
  recorded_at: number;
}
```

From packages/orchestrator/src/dolt/client.ts (imports used by queries.ts):
```typescript
import { query, execute, transaction } from "./client.js";
import type { RowDataPacket } from "mysql2";
```

From packages/orchestrator/src/commands/merge-order.ts (command module pattern):
```typescript
import type { Command } from "commander";
import Table from "cli-table3";
export function mergeOrderCommands(program: Command): void {
  program.command("merge-order").description("...").action(async () => { ... });
}
```

From packages/orchestrator/src/cli.ts (command registration pattern):
```typescript
import { mergeOrderCommands } from "./commands/merge-order.js";
mergeOrderCommands(program);
```

Legacy cost command to replace (cli.ts lines 207-226):
```typescript
program.command("cost").description("Show cost report for a cycle")
  .argument("<cycle-id>", "Cycle ID").action(async (cycleId: string) => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add developer cost query functions to queries.ts and implement tests</name>
  <files>
    packages/orchestrator/src/dolt/queries.ts
    test/unit/dolt/cost-queries.test.ts
  </files>
  <read_first>
    packages/orchestrator/src/dolt/queries.ts
    packages/orchestrator/src/dolt/schema.ts
    packages/orchestrator/src/dolt/client.ts
    test/unit/dolt/cost-queries.test.ts
    test/unit/commands/merge-order.test.ts
  </read_first>
  <behavior>
    - recordDeveloperCost({developer_id, phase_id, model, input_tokens, output_tokens, cost_usd}) inserts row and returns DeveloperCost with auto-increment id
    - getDeveloperCosts(developerId) returns DeveloperCost[] ordered by recorded_at DESC
    - getPhaseCosts(phaseId) returns DeveloperCost[] ordered by recorded_at DESC
    - getTeamCostSummary() returns array of {developer_id, phase_id, total_cost, total_input, total_output, record_count} grouped by developer_id, phase_id
    - getPhaseTotalCost(phaseId) returns number (sum of cost_usd for phase, 0 if no records)
  </behavior>
  <action>
1. Add a new section `// --- Developer Costs (v2) ---` to the end of queries.ts (before any closing content).
2. Add these 5 exported functions following the exact patterns in existing queries:
   - `recordDeveloperCost(params)`: INSERT INTO developer_costs with Date.now() for recorded_at, return object with insertId from result
   - `getDeveloperCosts(developerId)`: SELECT * WHERE developer_id = ? ORDER BY recorded_at DESC
   - `getPhaseCosts(phaseId)`: SELECT * WHERE phase_id = ? ORDER BY recorded_at DESC
   - `getTeamCostSummary()`: SELECT developer_id, phase_id, SUM(cost_usd) as total_cost, SUM(input_tokens) as total_input, SUM(output_tokens) as total_output, COUNT(*) as record_count FROM developer_costs GROUP BY developer_id, phase_id ORDER BY developer_id, phase_id
   - `getPhaseTotalCost(phaseId)`: SELECT COALESCE(SUM(cost_usd), 0) as total WHERE phase_id = ?, return Number(rows[0]?.total || 0)

3. Update test/unit/dolt/cost-queries.test.ts: replace it.todo stubs with real tests that mock client.ts (query, execute) and verify SQL calls and return values.

Use `vi.mock("../../../packages/orchestrator/src/dolt/client.js", ...)` to mock `query` and `execute`. The execute mock should return `{ insertId: 1 }` for recordDeveloperCost. The query mock should return appropriate arrays for the SELECT functions.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && npx vitest run test/unit/dolt/cost-queries.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "recordDeveloperCost" packages/orchestrator/src/dolt/queries.ts returns matches
    - grep "getDeveloperCosts" packages/orchestrator/src/dolt/queries.ts returns matches
    - grep "getTeamCostSummary" packages/orchestrator/src/dolt/queries.ts returns matches
    - grep "getPhaseTotalCost" packages/orchestrator/src/dolt/queries.ts returns matches
    - grep -c "it.todo" test/unit/dolt/cost-queries.test.ts returns 0
    - npx vitest run test/unit/dolt/cost-queries.test.ts exits 0
  </acceptance_criteria>
  <done>Five developer cost query functions exported from queries.ts. All 5 test stubs replaced with passing assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cost and dashboard CLI commands, wire into cli.ts, implement tests</name>
  <files>
    packages/orchestrator/src/commands/cost.ts
    packages/orchestrator/src/commands/dashboard.ts
    packages/orchestrator/src/cli.ts
    test/unit/commands/cost.test.ts
    test/unit/commands/dashboard.test.ts
  </files>
  <read_first>
    packages/orchestrator/src/commands/merge-order.ts
    packages/orchestrator/src/cli.ts
    test/unit/commands/merge-order.test.ts
    test/unit/commands/cost.test.ts
    test/unit/commands/dashboard.test.ts
    packages/orchestrator/src/dolt/queries.ts
  </read_first>
  <behavior>
    - costCommands registers "cost" command with optional [cycle-id] argument, --team flag, --dev flag
    - cost command with --team calls getTeamCostSummary and displays a cli-table3 table
    - cost command with --dev calls getDeveloperCosts and displays a cli-table3 table
    - cost command with cycle-id (no flags) calls getCycleCosts for backward compatibility
    - cost command default (no args, no flags) shows team summary
    - cost "record" subcommand calls recordDeveloperCost with provided args
    - Budget alert: when displaying cost data, if getPhaseTotalCost exceeds Number(process.env.FORGE_PHASE_BUDGET_USD), print warning
    - dashboardCommands registers "dashboard" command with --port option (default 8501)
    - dashboard command spawns "streamlit run" with correct app.py path using child_process.spawn
  </behavior>
  <action>
1. **Create packages/orchestrator/src/commands/cost.ts:**
   - Export `costCommands(program: Command): void`
   - Register command: `program.command("cost").description("Show cost report (team-wide or per-developer)").argument("[cycle-id]", "Legacy: show costs for a v0.1.0 cycle").option("--team", "Show team-wide cost summary").option("--dev <name>", "Show costs for a specific developer")`
   - Action handler logic:
     - If `cycleId` provided and no --team/--dev flags: call `queries.getCycleCosts(cycleId)` and display with same format as legacy cli.ts cost command (lines 211-225)
     - If `--team`: call `queries.getTeamCostSummary()`, display as cli-table3 table with columns [Developer, Phase, Total Cost, Input Tokens, Output Tokens, Records]
     - If `--dev <name>`: call `queries.getDeveloperCosts(name)`, display as cli-table3 table with columns [Phase, Model, Cost, Input, Output, Date]
     - Default (no args): same as --team
   - After displaying any cost data, check budget: for each unique phase_id in results, call `queries.getPhaseTotalCost(phaseId)`, compare against `Number(process.env.FORGE_PHASE_BUDGET_USD || "0")`. If budget > 0 and total > budget, print `"WARNING: Phase ${phaseId} cost ($${total.toFixed(2)}) exceeds budget ($${budget.toFixed(2)})"`
   - Register "record" subcommand: `program.command("cost:record").description("Record a developer cost entry").requiredOption("--dev <name>", "Developer ID").requiredOption("--phase <n>", "Phase number").requiredOption("--model <model>", "Model name").requiredOption("--input <n>", "Input tokens").requiredOption("--output <n>", "Output tokens").requiredOption("--cost <n>", "Cost in USD")` that calls `queries.recordDeveloperCost(...)` and prints confirmation
   - Use `import * as queries from "../dolt/queries.js"` and `import Table from "cli-table3"`

2. **Create packages/orchestrator/src/commands/dashboard.ts:**
   - Export `dashboardCommands(program: Command): void`
   - Register command: `program.command("dashboard").description("Launch the Streamlit team dashboard").option("--port <port>", "Port to run on", "8501")`
   - Action handler: resolve app.py path as `resolve(__dirname, "../../../../dashboard/app.py")` (from dist/commands/ back to packages/dashboard/). Verify this resolves correctly -- use `import.meta.url` if needed, but follow the existing pattern. Use `spawn("streamlit", ["run", appPath, "--server.port", opts.port], { stdio: "inherit", env: { ...process.env } })`. Handle "error" event to suggest `pip install streamlit`.
   - Print `"Launching Forge Dashboard on port ${opts.port}..."`

3. **Update packages/orchestrator/src/cli.ts:**
   - Add imports: `import { costCommands } from "./commands/cost.js"` and `import { dashboardCommands } from "./commands/dashboard.js"`
   - Add to v2 command groups section (after line 31): `costCommands(program);` and `dashboardCommands(program);`
   - Remove the legacy inline cost command (lines 207-226) since cost.ts now handles both legacy and v2 interfaces

4. **Update tests:**
   - Replace it.todo stubs in test/unit/commands/cost.test.ts with real tests. Mock queries.js functions. Follow the merge-order.test.ts pattern: extract action handler from commander mock, call it, capture console.log output, assert.
   - Replace it.todo stubs in test/unit/commands/dashboard.test.ts with real tests. Mock child_process.spawn. Verify spawns streamlit with correct args.
  </action>
  <verify>
    <automated>cd /Users/address0/Documents/Repos/forge && npx vitest run test/unit/commands/cost.test.ts test/unit/commands/dashboard.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "costCommands" packages/orchestrator/src/commands/cost.ts returns matches
    - grep "dashboardCommands" packages/orchestrator/src/commands/dashboard.ts returns matches
    - grep "costCommands" packages/orchestrator/src/cli.ts returns matches
    - grep "dashboardCommands" packages/orchestrator/src/cli.ts returns matches
    - grep -c "it.todo" test/unit/commands/cost.test.ts returns 0
    - grep -c "it.todo" test/unit/commands/dashboard.test.ts returns 0
    - The old inline cost command is removed from cli.ts (grep "Show cost report for a cycle" cli.ts returns nothing)
    - npx vitest run test/unit/commands/cost.test.ts test/unit/commands/dashboard.test.ts exits 0
  </acceptance_criteria>
  <done>cost.ts and dashboard.ts command modules created and exported. cli.ts updated with imports and registrations, legacy inline cost command removed. All test stubs replaced with passing tests.</done>
</task>

</tasks>

<verification>
cd /Users/address0/Documents/Repos/forge && npx vitest run test/unit/dolt/cost-queries.test.ts test/unit/commands/cost.test.ts test/unit/commands/dashboard.test.ts --reporter=verbose
All tests pass. Zero it.todo remaining.
</verification>

<success_criteria>
- 5 cost query functions in queries.ts (recordDeveloperCost, getDeveloperCosts, getPhaseCosts, getTeamCostSummary, getPhaseTotalCost)
- forge cost command supports --team, --dev, [cycle-id], and record subcommand
- Budget alert fires when FORGE_PHASE_BUDGET_USD exceeded
- forge dashboard spawns Streamlit on configurable port
- Legacy cost command removed from cli.ts, replaced by modular cost.ts
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-dashboard-and-cost-tracking/04-01-SUMMARY.md`
</output>
